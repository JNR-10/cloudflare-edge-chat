name = "edge-helpdesk"
main = "src/index.ts"
compatibility_date = "2025-10-31"
workers_dev = true

# Bind Workers AI as `env.AI`
[ai]
binding = "AI"

# Durable Objects binding for per-session memory
[[durable_objects.bindings]]
name = "SESSIONS"
class_name = "SessionDO"

# Durable Objects migrations (increment tag if you redeploy schema changes)
[[migrations]]
tag = "v1"
new_classes = ["SessionDO"]